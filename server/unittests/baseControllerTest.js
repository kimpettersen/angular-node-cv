var controller = require('../api/baseController'),
    should = require('should');


describe('Request handler for baseController', function(){
    var error;
    var result;
    var response = Object.create;
    var statusCode;
    // it('should return the status code passed', function(done){

    // });

    // it('should return 500 internal server error if there\'s an error', function(done){

    // });

    // it('should return 204 No content if there\'s no content', function(done){

    // });

    // it('should return a the result with status code 200 if some content exists', function(done){

    // });
});

